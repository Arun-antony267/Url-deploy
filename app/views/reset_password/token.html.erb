
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        .reset_password {
  width: 80%;
            /* height: 70vh; */
            max-width: 500px;
            margin: 0 auto; /* Center the container */
            background-color: #fff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 30vh; /* Adjust as needed */
        }
    </style>
    <title>Centered Container Example</title>
</head>
<body>
     <div id="header3"></div> 
 <%= render "layouts/flash"%>
<div class="reset_password">
<h4>Enter the token Recieved in Email</h4>
<%= form_tag :action => 'token_verify' do %>
<%# <div class="password"> %>
                     <label for="token">  </label><br> 
                     <input class="shortInput form-control" name="token" placeholder="Token"  type="text" ><br>
                     <button type="submit" class= "button" >Validate Token</button>
            <%# </div> %>
            <% end%>

<a href = "/">back</a>
</div>

</body>
</html>